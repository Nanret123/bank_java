package com.example.bank.audit.enums;

public enum OperationType {
    CREATE,
    UPDATE,
    DELETE,
    LOGIN,
    LOGOUT,
    TRANSACTION,
    ACCOUNT_FREEZE,
    ACCOUNT_UNFREEZE,
    BALANCE_INQUIRY
}